
# Application Settings
APP_ENV=development
SYSTEM_TIMEZONE=Africa/Cairo
DEFAULT_LANG=en

# MySQL Database Configuration
# For Docker: use 'db' as host (container name)
# For local: use 'localhost'
MYSQL_DB_HOST=localhost
MYSQL_DB_PORT=3306
MYSQL_DB_NAME=phplitecore
MYSQL_DB_USER=phplitecore
MYSQL_DB_PASS=secret
MYSQL_DB_CHAR=utf8mb4

# MySQL Root Password (used by Docker only)
MYSQL_ROOT_PASSWORD=rootsecret

# MSSQL Configuration (optional)
MSSQL_HOST=
MSSQL_PORT=
MSSQL_DATA=
MSSQL_USER=
MSSQL_PASS=

# SMTP Settings for Error Reporting
# For Docker development with MailHog: use 'mailhog' as host and port 1025
# For production: use your actual SMTP server
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your-username@example.com
SMTP_PASSWORD=your-app-password
SMTP_ENCRYPTION=tls
SMTP_FROM_ADDRESS=noreply@your-domain.com
SMTP_FROM_NAME="phpLiteCore Application"

# Docker Development Settings
# Uncomment these when using Docker Compose
# MYSQL_DB_HOST=db
# SMTP_HOST=mailhog
# SMTP_PORT=1025
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_ENCRYPTION=